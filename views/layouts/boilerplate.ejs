<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>PawPetss - Your Perfect Pet Awaits</title>
    <!-- SVG Favicon -->
    <link rel="icon" type="image/svg+xml" href="/paw.svg" />
    <!-- Fallback for older browsers -->
    <link rel="icon" type="image/png" href="/paw.png" />>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <link rel='stylesheet' href='https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css'>
  <script class="check-code" link="https://harshalmali.online"></script>
</head>

<!-- Premium Body with Gradient Background -->
<body class="bg-gradient-to-br from-emerald-50 to-white text-gray-800 font-sans min-h-screen flex flex-col relative overflow-x-hidden">
  <!-- Floating Background Elements -->
  <div class="fixed inset-0 pointer-events-none z-0">
    <div class="absolute top-20 left-10 w-32 h-32 bg-emerald-200/20 rounded-full blur-3xl animate-pulse"></div>
    <div class="absolute bottom-32 right-20 w-40 h-40 bg-emerald-300/15 rounded-full blur-2xl animate-pulse delay-1000"></div>
    <div class="absolute top-1/2 left-1/4 w-24 h-24 bg-emerald-100/30 rounded-full blur-xl animate-bounce delay-500"></div>
    <div class="absolute bottom-1/4 right-1/3 w-28 h-28 bg-emerald-200/25 rounded-full blur-lg animate-ping delay-2000"></div>
  </div>

  <!-- Enhanced Header with Premium Effects -->
  <header class="relative bg-gradient-to-r from-emerald-800 via-emerald-700 to-emerald-800 text-white py-8 px-4 shadow-2xl text-center z-20 overflow-hidden">
    <!-- Header Background Animation -->
    <div class="absolute inset-0 pointer-events-none">
      <div class="absolute top-0 left-0 w-64 h-64 bg-emerald-400/10 rounded-full blur-3xl animate-pulse"></div>
      <div class="absolute bottom-0 right-0 w-48 h-48 bg-emerald-300/10 rounded-full blur-2xl animate-pulse delay-1000"></div>
    </div>
    
    <!-- Animated bottom border -->
    <div class="absolute bottom-0 left-0 right-0 h-1 bg-gradient-to-r from-transparent via-emerald-400/60 to-transparent animate-pulse"></div>
    
    <div class="relative z-10">
      <!-- Brand Title with Animation -->
      <h1 class="text-5xl md:text-7xl font-extrabold mb-2 tracking-tight opacity-0 animate-fade-in-up">
        <span class="bg-gradient-to-r from-white via-emerald-100 to-white bg-clip-text text-transparent">PawPetss</span>
      </h1>
      <p class="text-xl md:text-2xl font-semibold mb-8 text-emerald-100 opacity-0 animate-fade-in-up animation-delay-300">Your Perfect Pet Awaits</p>

      <!-- Premium Search Bar -->
      <form class="max-w-lg mx-auto mt-6 opacity-0 animate-fade-in-up animation-delay-600">
        <label for="search" class="sr-only">Search pets or products</label>
        <div class="relative flex items-center group">
          <span class="absolute left-5 text-emerald-400 group-hover:text-emerald-300 transition-colors duration-300 z-10">
            <i class="fa fa-search text-lg group-hover:scale-110 transition-transform duration-300"></i>
          </span>
          <input
            type="search"
            id="search"
            placeholder="Search pets, accessories..."
            class="w-full pl-14 pr-32 py-4 bg-white/10 backdrop-blur-sm text-white placeholder-emerald-200 rounded-full border border-emerald-300/30 focus:outline-none focus:border-emerald-400 focus:ring-2 focus:ring-emerald-300/50 transition-all duration-300 hover:bg-white/20 focus:bg-white/20"
          />
          <button
            type="submit"
            class="absolute right-2 top-2 bottom-2 bg-emerald-600 hover:bg-emerald-500 focus:bg-emerald-500 text-white font-bold text-sm px-6 py-2 rounded-full shadow-lg transition-all duration-300 transform hover:scale-105 hover:-translate-y-0.5 focus:ring-2 focus:ring-emerald-400"
          >
            Search
          </button>
        </div>
      </form>
    </div>
  </header>

  <!-- Navigation Include -->
  <%- include("../includes/navbar.ejs") %>

  <!-- Premium Main Content Area -->
  <main class="bg-transparent flex-1 p-4 md:p-8 relative z-10">
    <div class="max-w-6xl mx-auto">
      <div class="min-h-[400px] opacity-0 animate-fade-in-up animation-delay-900">
        <%- body %>
      </div>
    </div>
  </main>

  <!-- Footer Include -->
  <%- include("../includes/footer.ejs") %>

  <!-- Custom CSS for Premium Effects -->
  <style>
    @keyframes fade-in-up {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    
    .animate-fade-in-up {
      animation: fade-in-up 0.8s ease-out forwards;
    }
    
    .animation-delay-300 {
      animation-delay: 0.3s;
    }
    
    .animation-delay-600 {
      animation-delay: 0.6s;
    }
    
    .animation-delay-900 {
      animation-delay: 0.9s;
    }
    
    /* Glass morphism effect */
    .backdrop-blur-sm {
      backdrop-filter: blur(8px);
    }
    
    /* Enhanced shadow effects */
    .shadow-2xl {
      box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25), 0 0 0 1px rgba(16, 185, 129, 0.05);
    }
    
    /* Smooth scrolling */
    html {
      scroll-behavior: smooth;
    }
    
    /* Custom gradient text */
    .bg-clip-text {
      -webkit-background-clip: text;
      background-clip: text;
    }
    
    /* Prevent horizontal scroll */
    body {
      overflow-x: hidden;
    }
  </style>
</body>

</html>
